#include <stdio.h>

#define f(i,n) for(int i=0;i<n;i++)

int main(){
    int m,n,count1=0,count2=0;
    scanf("%d%d",&m,&n);
    int matrix[m][n];

    f(i,m){
        f(j,n)scanf("%d",&matrix[i][j]);
    }

    f(i,m){
        f(j,n){
            
        }
    }

    return 0;
}